spring.application.name=ProjectVM

# URL del RDS MySQL
spring.datasource.url=jdbc:mysql://vinvulationmediun.cfmmag2i2up4.us-east-1.rds.amazonaws.com:3306/vinvulationmediun?useSSL=false&serverTimezone=America/Lima

# Usuario y contraseña del RDS
spring.datasource.username=admin
spring.datasource.password=2025DTI.

# Configuración JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Zona horaria y formato de fechas
spring.jackson.time-zone=America/Lima
spring.jackson.date-format=yyyy-MM-dd

# Configuración de archivos (si tu app lo usa)
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Desactiva config server si no lo usas
spring.cloud.config.enabled=false
